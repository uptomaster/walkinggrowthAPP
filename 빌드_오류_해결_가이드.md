# 빌드 오류 해결 가이드

## ✅ 해결된 문제

### 1. Java 버전 오류 해결
- `compileSdkVersion`을 36에서 34로 변경 (Java 17 호환)
- `build.gradle`에 Java 17 설정 추가
- `gradle.properties`에 경고 억제 설정 추가

### 2. npm 스크립트 오류 해결
- 올바른 명령어 안내

## 🚀 올바른 빌드 순서

### 1단계: 웹 파일 준비 및 동기화

**프로젝트 루트에서:**
```bash
npm run prepare-android
npm run cap:sync
```

또는 한 번에:
```bash
npm run cap:sync
```

### 2단계: 안드로이드 스튜디오에서 빌드

**방법 A: Gradle 명령어 (권장)**

프로젝트 루트에서:
```bash
cd android
.\gradlew clean
.\gradlew assembleDebug
```

**방법 B: 안드로이드 스튜디오 GUI**

1. 안드로이드 스튜디오에서 `android` 폴더 열기
2. **Build** → **Clean Project**
3. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**

## 📝 변경 사항

### gradle.properties
- `android.suppressUnsupportedCompileSdk=36` 추가 (경고 억제)

### app/build.gradle
- `compileOptions` 추가 (Java 17 설정)

### build.gradle
- `tasks.withType(JavaCompile)` 추가 (Java 17 설정)

### variables.gradle
- `compileSdkVersion`: 36 → 34
- `targetSdkVersion`: 36 → 34

## ⚠️ 중요 사항

1. **Java 버전**: Java 17 이상 필요
   - 안드로이드 스튜디오는 자동으로 JDK를 사용합니다
   - 문제가 있으면 안드로이드 스튜디오의 JDK 설정 확인

2. **Clean 빌드**: 첫 빌드 전에 `clean` 실행 권장
   ```bash
   .\gradlew clean assembleDebug
   ```

3. **npm 스크립트**: 올바른 명령어 사용
   - ❌ `npm run cap sync android` (잘못됨)
   - ✅ `npm run cap:sync` (올바름)
   - 또는: `npx cap sync android`

## 🎯 빠른 빌드 명령어

**전체 프로세스:**
```bash
# 프로젝트 루트에서
npm run prepare-android
npm run cap:sync
cd android
.\gradlew clean assembleDebug
```

**빌드만:**
```bash
cd android
.\gradlew clean assembleDebug
```

## 💡 빌드 성공 후

빌드된 APK 위치:
```
android/app/build/outputs/apk/debug/WalkStory-debug.apk
```

설치:
```bash
adb install android/app/build/outputs/apk/debug/WalkStory-debug.apk
```
