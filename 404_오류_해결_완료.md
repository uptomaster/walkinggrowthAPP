# 404 NOT_FOUND 오류 해결 완료

## 🔴 문제 원인

Vercel 서버리스 함수는 중첩된 폴더 구조(`api/auth/kakao-oauth/start.js`)를 제대로 인식하지 못했습니다.

## ✅ 해결 방법

**단일 파일 구조로 변경:**

### 변경 전:
```
api/auth/kakao-oauth/
  ├── start.js      → /api/auth/kakao-oauth/start (404 오류)
  └── callback.js   → /api/auth/kakao-oauth/callback (404 오류)
```

### 변경 후:
```
api/auth/
  ├── kakao-oauth-start.js      → /api/auth/kakao-oauth-start ✅
  └── kakao-oauth-callback.js   → /api/auth/kakao-oauth-callback ✅
```

## 📝 수정된 파일

1. **`api/auth/kakao-oauth-start.js`** (새로 생성)
   - `/api/auth/kakao-oauth-start` 엔드포인트

2. **`api/auth/kakao-oauth-callback.js`** (새로 생성)
   - `/api/auth/kakao-oauth-callback` 엔드포인트

3. **`app.js`** (수정)
   - 경로 변경: `/api/auth/kakao-oauth/start` → `/api/auth/kakao-oauth-start`

4. **`www/app.js`** (수정)
   - 경로 변경: `/api/auth/kakao-oauth/start` → `/api/auth/kakao-oauth-start`

5. **`api/auth/kakao-oauth.js`** (수정)
   - 리다이렉트 URI 변경: `/api/auth/kakao-oauth/callback` → `/api/auth/kakao-oauth-callback`

## 🚀 다음 단계

### 1. Vercel에 배포 (필수)

변경사항을 Git에 커밋하고 푸시:
```bash
git add .
git commit -m "Fix Kakao OAuth API paths for Vercel"
git push
```

Vercel이 자동으로 배포합니다.

### 2. 카카오 개발자 콘솔 업데이트 (필수)

1. [카카오 개발자 콘솔](https://developers.kakao.com/) 접속
2. **내 애플리케이션** → **WalkStory** 앱 선택
3. **제품 설정** → **카카오 로그인** 클릭
4. **Redirect URI 등록** 섹션에서:
   - ❌ 기존 URI 삭제: `https://walkinggrowth-app.vercel.app/api/auth/kakao-oauth/callback`
   - ✅ 새 URI 추가: `https://walkinggrowth-app.vercel.app/api/auth/kakao-oauth-callback`

### 3. 배포 확인

배포 완료 후 다음 URL이 작동하는지 확인:
```
https://walkinggrowth-app.vercel.app/api/auth/kakao-oauth-start
```

예상 응답:
```json
{
  "authUrl": "https://kauth.kakao.com/oauth/authorize?...",
  "state": "..."
}
```

### 4. 모바일 앱 테스트

- APK 재설치 (선택사항)
- 카카오 로그인 다시 시도

## ⚠️ 중요 사항

- **Vercel 배포 필수**: 변경사항이 배포되어야 합니다
- **카카오 리다이렉트 URI 변경 필수**: 개발자 콘솔에서 반드시 업데이트해야 합니다
- **웹과 앱 모두 동일한 API 사용**: 이제 웹과 앱 모두 같은 엔드포인트를 사용합니다

## 🔍 디버깅

### Vercel 함수 로그 확인

1. Vercel 대시보드 접속
2. **Deployments** → 최신 배포 선택
3. **Functions** 탭에서 로그 확인
4. `/api/auth/kakao-oauth-start` 함수 호출 여부 확인

### 네트워크 오류 확인

모바일 앱에서:
1. 개발자 도구 또는 Logcat에서 네트워크 요청 확인
2. `API_BASE` 값 확인: `https://walkinggrowth-app.vercel.app`
3. 요청 URL 확인: `https://walkinggrowth-app.vercel.app/api/auth/kakao-oauth-start`
